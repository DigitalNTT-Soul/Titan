local function roll(client, logger, message, text)

end

local function max(client, logger, message, text)

end

local function min(client, logger, message, text)

end

local function acon(client, logger, message, text)

end

local function acoff(client, logger, message, text)

end

return {
    roll = roll,
    max = max,
    min = min,
    acon = acon,
    acoff = acoff,
}