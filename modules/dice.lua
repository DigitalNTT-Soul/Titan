local function roll(client, core, message)

end

local function max(client, core, message)

end

local function min(client, core, message)

end

local function acon(client, core, message)

end

local function acoff(client, core, message)

end

return {
    roll = roll,
    max = max,
    min = min,
    acon = acon,
    acoff = acoff,
}